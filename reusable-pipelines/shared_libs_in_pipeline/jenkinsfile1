library identifier: 'Jenkins_Pipeline_Shared_Libs@main',
        retriever: modernSCM([$class: 'GitSCMSource', remote: 'https://github.com/Sri507/Jenkins_Pipeline_Shared_Libs.git'])

pipeline {
    agent any
tools {
        maven 'maven-3.6.3'
    }
    stages {
        stage('Audit tools') {
            steps {
                auditTools()
            }
        }
    }
}
